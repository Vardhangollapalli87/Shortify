services:
  api:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: shortify-api
    ports:
      - "5000:5000"
    env_file:
      - ./backend/.env
    restart: unless-stopped
